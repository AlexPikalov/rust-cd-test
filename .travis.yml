language: rust
script:
  - cargo test --verbose --all
deploy:
  # deploy master to production
  - provider: script
    script: cargo publish --token $PUBLISH_TOKEN
    on:
      tags: true
      branch: master
